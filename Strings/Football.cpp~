#include <iostream>
#include <string>
using namespace std;

int main() {
    string str;
    cin >> str;

    int count1 = 1, count0 = 1;
    for ( auto ch : str ) {
	ch == '1' ? count1++ : count0++;
    }
    cout << "NO\n";
}
    
